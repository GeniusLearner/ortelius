<project name="JunitReport">

  <!-- set global properties for this build -->
  <property name="project_name" value="junitReport"/>
  <property name="res"  location="${wd}/res"/>
  <property name="reports" location="${wd}/reports"/>
  
  <!-- the names of various distributable files -->
  <property name="jar_name" value="${project_name}.jar"/>
  <property name="war_name" value="${project_name}.war"/>

	<!-- top level targets -->

  <target name="clean"
        description="clean up" >
    <!-- Delete the ${classes.dir} and ${dist} directory trees -->
    <delete dir="${reports}"/>
  </target>

	<target name="run-tests" description="run your test suite" >
		
		<junit printsummary="yes" haltonfailure="no" showoutput="yes" >
			<classpath>
				_TOOL_CP_
			</classpath>
			
			<batchtest fork="yes" todir="${reports}/raw/">
		  		<formatter type="xml"/>
		    		<fileset dir="${src}">
		      			_INCLUDE_
							   
		  			_EXCLUDE_
		    		</fileset>
		  	</batchtest>
		</junit>	
	</target>
	
  <target name ="test" depends="run-tests">
		<junitreport todir="${reports}">
		  <fileset dir="${reports}/raw/">
		    <include name="TEST-*.xml"/>
		  </fileset>
		  <report format="noframes" todir="${reports}/html/"/>
		</junitreport>
  </target>
  
  <target name ="report" depends="">
		<junitreport todir="${reports}">
		  <fileset dir="${reports}/raw/">
		    <include name="TEST-*.xml"/>
		  </fileset>
		  <report format="noframes" todir="${reports}/html/"/>
		</junitreport>
  </target>
  
  <target name ="run" depends="" description="if this project can be run, run it" >
  	
  </target>

	<!-- supporting targets -->
	
  <target name="init" description="initialize the build environment" >
    <!-- Create the time stamp -->
    <tstamp/>
    <!-- Create directory structures -->
    <mkdir dir="${reports}"/>
    <mkdir dir="${reports}/raw/"/>
    <mkdir dir="${reports}/html/"/>
  </target>
  
  <target name="cleanbuild" depends="clean, init, test">
  	
  </target>
  
</project>

